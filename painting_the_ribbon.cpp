#include<bits/stdc++.h>
#include<iostream>
using namespace std;
int main()
{
    int n,t,m,k;
    cin >> t ;
    if (t>=1 && t<=1000)
    {
        for(int i=1 ; i<=t ;i++)
        {
            cin >> n >> m >> k ;
            if(m>=1 && n>=k && n<= 50)
            {
            
            }
        }
    }
}